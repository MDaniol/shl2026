# Feature-extraction environment (uv venv "SHL-local").
# Core (required by the scripts):
numpy>=2.0
scipy>=1.13
pyarrow>=16
tqdm>=4.66
# Analysis / downstream baseline (not needed for extraction itself):
pandas>=2.0
scikit-learn>=1.4
# Optional external feature library (catch22 ablation; compiles a C extension):
pycatch22>=0.4
# Differential validation (validate_features.py) — independent reference libs:
tsfel>=0.1
statsmodels>=0.14
