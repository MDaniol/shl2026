# SHL-local environment for ACK Cyfronet Athena (x86, CUDA A100).
# Mirrors the working local pins. momentfm + mantis-tsfm are installed
# separately with --no-deps (see setup_athena_env.sh) because their pins drag in
# an unbuildable old transformers/tokenizers.
torch                       # PyPI wheels bundle CUDA 12; works on Athena GPU nodes
transformers==4.44.2
huggingface_hub==0.25.2
tokenizers>=0.19,<0.20
safetensors
einops
datasets                    # required by mantis.trainer
numpy>=2.0
scipy>=1.13
pyarrow>=16
pandas>=2.0
scikit-learn>=1.4
lightgbm>=4.5
tqdm>=4.66
# feature-branch + validation libs
tsfel>=0.1
statsmodels>=0.14
pycatch22>=0.4
