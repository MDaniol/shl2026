# 28-06 Morning Plan — frozen-FM experiments to push past 0.8342 (overnight research synthesis)

**For the team lead to review + run one-by-one.** Built overnight from a 4-agent deep-research round
(har-fm ×2: embedding-surgery + frozen-backbone boundary; har-dl ×2: head/decision-layer + past-SHL
literature) plus a HAR-agent code-validation pass. Every experiment respects the guardrails: frozen FM,
lightweight heads only, temporal+embargo split, **select on TUNE / lock TEST once**, per-class F1,
MLflow-traced, deterministic. The 0.8342 champion submission is the safe fallback throughout.

---

## 0. TL;DR — the honest picture
- **All four agents converged on the same ceiling:** IMU-only + per-window + frozen-FM can *shave* the
  two macro-F1 caps (Train↔Subway, Car↔Bus), **not break them**. The SHL organizers themselves quantified
  that the historical gains came from HMM/temporal smoothing (~+10pp), GPS/barometer, and 1-min frames —
  **all forbidden to us**. Realistic envelope above 0.8342 is **low-single-digit macro**; the bigger payoff
  is a *rich, well-cited paper* ("we experimented a lot, disciplined, and characterized the ceiling").
- **Highest expected value, lowest risk, runnable now (CPU):** (A1) pair-separability diagnostic — already
  built; (C1) the **additive-logit decision layer** — built tonight. Both pure-CPU, no re-extraction.
- **Highest representation upside (needs 1 GH200 re-extraction):** **intermediate-layer extraction** on
  mantis + AST (both agents' #1 lever; final-layer over-compresses).
- **Biggest paper-novelty:** **LST ladder side-network** (frozen backbone, zero gradient through it —
  the cleanest "frozen" claim) + **CKA-gated representation fusion** (explains *why* E-FMDIV diluted).
- **Cheap, physically-grounded, additive feature packs:** diesel-10Hz/gasoline-25Hz idle bands (Car/Bus)
  + magnetometer rail pack (Train/Subway) — but **audit the existing 520 first** (substantial overlap).

---

## 1. State going in
- **Champion = 0.8342** (locked, BHT-only TEST): calibrated soft-vote `utica_V2 + mantisv2_V1`, each =
  `FM-emb ⊕ 520 handcrafted → LightGBM`, per-class calibrated (`calibrate()` = macro-F1 **multiplicative**
  per-class reweight on TUNE), weighted vote + recal. Best single utica 0.823.
- **Per-class:** Run ≈0.96 (saturated) but **under-emitted on test (1.2% pred vs 1.91% true)**; caps are
  **Train ~0.72 / Subway ~0.74** and **Car ~0.90 / Bus ~0.84**.
- **DISABLED (do not revisit):** per-location MoE, rail expert, vehicle expert, Tier-1 post-hoc,
  cross-channel SE head (+0.006), **E-FMDIV diversity voters** (dinov2/imagebind/ast — all dilute & overfit
  TUNE weights). Ruled-out FMs: oneHAR/MASTER/SensorLM/LSM/UniMTS/CrossHAR/PRIMUS.
- **Two structural facts that kill several "obvious" ideas:**
  1. **The head is a tree.** LightGBM is invariant to per-feature scaling and *hurt* by rotations →
     whitening/PCA/ICA/RFF/learned-layer-weighted-sum **only help a linear/kNN/MLP sub-model**, never the tree.
  2. **Transforms help only if they** (a) surface signal the encoder kept but pooling discarded
     (intermediate layers, token pooling), or (b) inject *supervised* pair-direction scalars (Fisher/LDA)
     a greedy tree can't reconstruct. Re-projecting what the tree already sees is dead weight.

---

## 2. The experiment batch — run top-to-bottom, paste results, decide

Legend: **[BUILT]** = code + test + sbatch committed, runnable now · **[SPEC]** = designed, needs a short
build (we'll do together) · gate = **KEEP iff TEST > 0.8342** (per-class reported), selected on TUNE.

### Tier A — runnable now, CPU, highest ROI/effort

**A0. Re-confirm the champion bar + regenerate the fallback submission [BUILT]** *(also closes #18)*
```bash
cd ~/shl2026 && git pull
BAR=$(EMBS=utica_V2,mantisv2_V1 sbatch --parsable notebooks/mdaniol/hpc/voting_head.sbatch)   # deterministic 2-way bar
SUB=$(sbatch --parsable notebooks/mdaniol/hpc/submit_vote_helios.sbatch)                       # AGH_predictions_v3_vote.txt (fallback)
```
Why: utica shifted 0.8213→0.8229 with the determinism fix; lock the true post-fix bar and refresh the
submittable champion. Everything below is judged against this re-confirmed number.

**A1. Pair-separability diagnostic [BUILT] — decides the whole hard-pair program**
```bash
A1F=$(EMB=utica_V2 REP=fusion      sbatch --parsable notebooks/mdaniol/hpc/pair_separability_helios.sbatch)
A1H=$(EMB=utica_V2 REP=handcrafted sbatch --parsable notebooks/mdaniol/hpc/pair_separability_helios.sbatch)
```
Output `PAIR_SEPARABILITY_utica_V2_{fusion,handcrafted}.md`. **Read first.** Binary linear probe, fit on
FIT, scored on locked TEST. **≥0.80 bal-acc ⇒ SEPARABLE** → build B1/E (Fisher columns, soft hierarchy).
**≤0.65 ⇒ COLLAPSED** → the ceiling is an information limit; **skip the hard-pair lane** and write it up.
The fusion-vs-handcrafted delta says whether the FM adds pair-separating signal at all.

**C1. Additive-logit decision layer [BUILT] — the single best CPU lever**
```bash
DEC=$(EMBS=utica_V2,mantisv2_V1 sbatch --parsable notebooks/mdaniol/hpc/decision_rule_helios.sbatch)
```
Output `DECISION_RULE_RESULTS.md`. Tests `baseline / mult_reweight(champion) / additive_logit /
mult+additive / sld_gated`, each selected on TUNE, locked on TEST. **Hypothesis:** the current
*multiplicative* reweight can't lift Run at p→0 (a vanishing multiplier); the **additive log-bias**
`argmax(log p_k + b_k)` (Bayes-optimal post-hoc, GLA 2310.08106; joint coordinate-descent, Lipton
1402.1892) can — fixing the Run under-emission and nudging the weaker member of each pair. Pure CPU, no
refit of representations. **SLD is secondary + flagged** (reads the global test-pool prior → verify the
challenge permits unsupervised whole-test statistics before shipping it; it uses no per-window/temporal info).

> Run **A0 + A1 + C1 first.** They are cheap, safe, and C1 is the most likely small win + best paper angle.

### Tier B — cheap, CPU, additive (build small, then run)

**B1. Hard-pair Fisher / LDA scalar columns [SPEC, ~1 h build] — gated on A1 = SEPARABLE**
Append `wᵀx` (1-D Fisher axis `w ∝ S_w⁻¹(μ_Train−μ_Subway)`, fit on FIT only, refit per fold) and the
Car/Bus axis as ~2–4 new tree columns. Trees can't cheaply reconstruct a rotated linear combo, so a
*supervised* discriminant axis is genuine new pair-signal (LDA-into-GBDT 1806.07506). Targets both caps.

**B2. Physical feature packs — diesel/gasoline idle + magnetometer rail [SPEC, ~half-day build]**
- *Audit first:* `shl_features.py` already has 42 subband energy/ratio features + mag_mag/mag_rate_mag +
  6 spectral scalars — so **add only the genuinely missing**: spectral *entropy* (road broadband vs rail
  tonal), explicit idle bands (~10 Hz diesel-bus vs ~25 Hz gasoline-car firing freq, both < 50 Hz Nyquist,
  resolvable in 5 s), per-axis magnetometer std + 0–10 Hz band-power + low-band entropy (Sensors 2021
  21(1):176: magnetometer's *best-separated* pair is exactly Subway-vs-Train).
- *Caveats:* the idle signature exists only in *stopped* windows; the 16.7 Hz AC-rail feature is EU-only
  (**verify dataset geography before adding it**). Recompute features → re-run bake-off/vote.

**B3. CKA-gated representation fusion [SPEC, ~half-day] — also explains E-FMDIV's failure (paper)**
Compute centered linear-CKA (Kornblith 1905.00414) across {utica, intermediate-mantis, dinov2, ast,
imagebind}; **concatenate into ONE LightGBM only the pair that is low-CKA AND individually competent**
(representation-level fusion ≠ the late vote that diluted). If no pair qualifies → clean negative +
publishable explanation of why naive diversity voting failed.

### Tier C — representation upside (1 GH200 re-extraction)

**C2. Intermediate-layer extraction, mantis + AST [SPEC, ~1 day] — both agents' #1 lever**
Final layer over-compresses to the pretraining objective; intermediate (40–70% depth) probes better
(TiViT 2506.08641; molecular 2506.06443 up to +28.6%). Add `output_hidden_states`/`return_transf_layer`
to `extract_embeddings.py`, extract layers {AST: 5,7,9 of 12; mantis: 2,3,4 of 6}, **tree path = select
best single raw layer on TUNE** (not a weighted sum — that's a rotation), lock TEST. *UTICA is blocked
(no public code); MOMENT medium (re-pool per layer).* Highest representation EV; paper-novel for TS-FMs.

### Tier D — novelty / differentiable heads (after C2's cached layers; CPU training)

**D1. Trainable layer-attention probe [SPEC] — ALLOWED (forward-only on cached layers)**
Head learns softmax weights over the cached layers → weighted-sum → small MLP head. (Weighted-sum is a
rotation → MLP, not the tree.) Add as an ensemble member.

**D2. LST ladder side-network with cross-channel attention [SPEC] — HIGHEST paper-novelty**
A lightweight side-net reads cached frozen intermediate activations via ladder connections and does
**self-attention over the 9 per-channel embedding tokens** (the cross-channel coupling the mean discards).
LST is *defined* by **no backprop through the backbone** (2206.06522) → the cleanest possible "frozen"
claim and a direct answer to "freeze weights, adjust the heads." Add to the ensemble; gate on TEST.

**D3. FiLM head gating 520-HC × FM-emb [SPEC, cheapest] — ALLOWED**
Tiny MLP maps per-window handcrafted stats → (γ,β); apply `γ⊙e+β` to the cached embedding before the
head — finer than concat, can silence noise dims (FiLM 1709.07871). Low-regret ensemble diversifier.

### Tier E — hard-pair classifier tricks (gated on A1 = SEPARABLE)

**E1. Soft coarse→fine hierarchy [SPEC] — preferred over a pair specialist**
Stage-1 coarse {motion / vehicle / rail} posterior × stage-2 within-cluster head, combined by **posterior
product, never hard routing** (avoids the MoE/router overfit we already hit). Run the coarse-confusion
diagnostic first; if the coarse split isn't cleaner than flat 8-way, abort. (Ashqar 2006.06945 is
hierarchical-Bayes, not a pairwise override — soft hierarchy outranks the gated-pair specialist.)

### Tier F — augmentation probe (1 GH200 re-embed; uncertain ROI under frozen)

**F1. Gravity-aware-rotation augmentation [SPEC]**
Rotate raw windows about the gravity axis + small tilt → re-embed through the frozen FM **and recompute
all 520 features** (keep them self-consistent) → extra head-training samples; + selective TTA on
low-confidence windows. Targets the Bag/Hips/Torso TUNE→TEST gap; time-warp variant for Run. **Avoid
magnitude-altering augs** (magnitude is class-defining). Honest: frozen-FM gains ≪ the fine-tuning numbers
in the literature — treat as a probe.

---

## 3. Frozen-FM compliance boundary (for the paper's "frozen" claim)
Two tests: **(A)** modifies any backbone param (weights/bias/its LayerNorm affines)? **(B)** requires
gradient *through* the frozen backbone? Strict "frozen" = **pass both**.

| Technique | Verdict |
|---|---|
| Linear/any head on cached emb; **LST side-net**; layer-attention probe; FiLM-on-cached-emb | **ALLOWED ✓** (forward-only) |
| BitFit, LayerNorm/affine tuning, LP-FT, full fine-tuning | **FORBIDDEN** (modify backbone params) |
| SSF, LoRA (merge into weights); adapters, prompt/prefix/VPT (backprop through backbone) | **GRAY → avoid** |
| Learned input-adapter trained via grad-through-FM | **defer**; use *label-free* gravity-canonicalization instead |

**Paper line:** state explicitly that adapters/LoRA/prompt/SSF/BitFit/LN-tuning were *deliberately excluded*
to preserve a forward-only frozen guarantee — all trainable params live outside the backbone with no
gradient through it. LST + layer-attention + FiLM-on-cached-emb instantiate exactly that.

---

## 4. Cross-cutting discipline (applies to every experiment)
- **Gate:** KEEP into the submission **iff TEST macro > re-confirmed bar**, selected on TUNE, locked once.
  Report **per-class** (Run + Train/Subway + Car/Bus). Nothing ships on a TUNE win alone.
- **Overfit guard:** for any multi-param selection (decision biases, stacker weights, hierarchy), require
  **nested-TUNE-CV gain > across-fold std** — the rule that retroactively explains the rail-expert and
  E-FMDIV failures.
- **Known OOF-leakage to fix (har-dl):** vote weights are currently selected on the same TUNE the base
  models early-stopped on → mild optimism. A GroupKFold-OOF combiner is the clean fix (eval hygiene for
  the paper; unlikely to move the number with 2 correlated bases).
- **Split-file note:** only `val_split.npy` is in git; jobs default to `val_split_temporal.npy`. Every
  sbatch here **regenerates it** if absent (`split.py --scheme temporal --embargo 100`). Confirmed.
- **MLflow + artifact snapshot** on every run; pre-register in `LIGHTWEIGHT_HEAD_PLAN.md`; diary conclusion.

---

## 5. Honest ceiling + paper narrative
- **Train↔Subway:** magnetometer pack (B2) is the single most credible new lever (one direct empirical
  citation) — expect single-digit-percent, not a transformation; accel vibration bands carry comparable weight.
- **Car↔Bus:** idle-band spectral signature (B2) realistically helps only on *stopped* windows.
- **Run:** a **decision/thresholding** problem (C1), not a feature problem — already saturated at ~0.96.
- **The paper writes itself either way:** intermediate-layer TS-FM probing (C2), the LST frozen-side-net
  (D2), CKA-gated fusion explaining the diversity-voter failure (B3), and a rigorous characterization of
  the IMU-only per-window ceiling — that's a strong "we experimented a lot, disciplined" story.

---

## 6. Suggested morning run order
1. **A0** (bar + fallback submission) — always have a submittable champion.
2. **A1** (read `PAIR_SEPARABILITY_*`) — decides whether B1/E are worth it.
3. **C1** (read `DECISION_RULE_RESULTS.md`) — most likely small win; ship if it clears the bar per-class.
4. If A1 = SEPARABLE → ping me to build **B1** then **E1**. If COLLAPSED → skip them, go to C2.
5. **B2 audit + C2 extraction** (the GH200 representation lever) — ping me to build; biggest upside + paper.
6. **B3 / D1 / D2 / D3 / F1** — novelty + ensemble breadth, build as budget allows.

*Status: A0/A1/C1 are committed and runnable. B1–F1 are specs with designs above — say the word per item
and I'll build (tested, pre-registered, gated) so you can run them the same way.*
